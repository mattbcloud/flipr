# Firebase App Hosting Configuration for Static HTML

# No build needed for static HTML
buildConfig:
  runtime: nodejs18

# Serve static files directly
runConfig:
  # Deploy Cloud Functions separately
  functions:
    source: functions
    
# Serve your index.html
staticAssets:
  # Serve index.html from root
  - source: index.html
    glob: index.html